# Optional faster OCR (may not install on very new Python versions).
# Use after: pip install -r requirements.txt
opencv-python-headless>=4.8.0
onnxruntime>=1.16.0
rapidocr-onnxruntime>=1.3.18

# Alternative ML OCR (large download):
# easyocr
